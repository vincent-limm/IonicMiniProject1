export interface Cpu {
    id: string,
    merek: string,
    model: string,
    foto: string,
    base_clock: number,
    boost_clock: number,
    core: number,
    thread: number,
    stock: number,
    harga: number,
}